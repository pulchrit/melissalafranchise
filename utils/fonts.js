import { Courier_Prime, Inter, Special_Elite } from 'next/font/google';

export const bodyFont = Courier_Prime({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const subHeadFont = Inter({
  subsets: ['latin'],
  weight: '900',
});

export const highlightFont = Special_Elite({
  subsets: ['latin'],
  weight: '400',
});
